apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: agentic-tools
  annotations:
    metallb.universe.tf/address-pool: homelab-pool
spec:
  type: LoadBalancer
  selector:
    app: openclaw
  ports:
  - name: gateway
    port: 18789
    targetPort: 18789
