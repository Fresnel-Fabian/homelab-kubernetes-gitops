apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llama-server-models
  namespace: llm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 20Gi